// import { create } from "zustand";

// interface State {
//   step: number;
// }

// interface Actions {
//   increaseStep: () => void;
//   decreaseStep: () => void;
//   resetStep: () => void;
// }

// const stepStore = create<State & Actions>((set) => ({
//   step: 0,
//   increaseStep: () => set((state) => ({ step: state.step + 1 })),
//   decreaseStep: () => set((state) => ({ step: state.step - 1 })),
//   resetStep: () => set({ step: 0 }),
// }));

// export default stepStore;
